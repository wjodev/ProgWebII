{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{#each acertos}}
    <div class="alert alert-success">{{texto}}</div>
{{else}}

{{/each}}

{{/each}}

{{#each imovel}}


<div class="card bg-light mb-3" style="max-width: 18rem;">
  <div class="card-header"><h5>INFORMAÇÕES IMÓVEL</h5></div>
  <div class="card-body">
    <h6 class="card-title">Matricula:</h6>
    <p class="card-text">{{this.matricula}}</p>
    <h6 class="card-title">CEP:</h6>
    <p class="card-text">{{this.cep}}</p>
    <h6 class="card-title">Endereço:</h6>
    <p class="card-text">{{this.endereco}}</p>
    <h6 class="card-title">Cidade:</h6>
    <p class="card-text">{{this.cidade}}</p>
    <h6 class="card-title">Estado:</h6>
    <p class="card-text">{{this.estado}}</p>
  </div>

  
        
<div class="form-group col-md-1">
      <form action="/imoveis/deletarImovel" method="post">
          <input type="hidden" name="id", value="{{_id}}">
          <button class="btn btn-danger">Deletar</button></a>     
      </form>
</div>
<div class="form-group col-md-1">
    <form action="/imoveis/editarImovel" method="post">
        <input type="hidden" name="id", value="{{_id}}">
        <button class="btn btn-primary">Editar</button></a> 
    </form>  
      <!--<a href= "/alunos/editarAluno/{{_id}}"><button class="btn btn-primary">Editar</button></a>-->
</div>
    

{{else}}
    <h5>Nenhum imóvel cadastrado</h5>
{{/each}}
